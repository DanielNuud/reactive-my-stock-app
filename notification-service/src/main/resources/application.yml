spring:
  application:
    name: notification-service
  r2dbc:
    url: r2dbc:postgresql://notification-db:5432/notification-db-rx
    username: postgres
    password: postgres
  flyway:
    enabled: true
    url: jdbc:postgresql://notification-db:5432/notification-db-rx
    user: postgres
    password: postgres

server:
  port: 8080